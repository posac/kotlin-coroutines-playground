package pl.posac.kotlin.coroutines.examples.raw_coroutines

fun `Introduction to coroutines? How they work, what is given purely in lang`() {
    `Introcution to simple coroutine, continuation`
    `Suspending functions - how they are working` {
        `Under the hood - step 1`()
        `Under the hood - step 2`()
    }
}